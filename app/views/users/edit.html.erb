<% provide :title, t("users.edit_user") %>
<% provide :button_text, t("users.save") %>
<h1><%= t "users.edit_user" %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("change"), Settings.user.mail_url, target: "_blank" %>
    </div>
  </div>
</div>
